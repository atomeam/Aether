# =============================================================================
# AETHER WORKSPACE - Cloudflare Workers Configuration
# =============================================================================
#
# NOTE: This workspace consists of:
#   - apps/backend (Node.js Express server - NOT a Worker)
#   - apps/bridge (Cloudflare Worker - has own wrangler.toml)
#   - apps/homebase (Cloudflare Worker - has own wrangler.toml)
#
# Each worker has its own wrangler.toml in apps/*/wrangler.toml
# This root wrangler.toml is deprecated - do not use for deployment
# =============================================================================

# DEPRECATED: apps/backend is a Node.js Express server, not a Cloudflare Worker
# Use: npm run dev:backend (port 3000) or npm run build
# Individual workers are deployed from their own wrangler.toml files
