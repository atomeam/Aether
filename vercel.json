{
  "version": 2,
  "buildCommand": "npx turbo run build --filter=@aether/backend",
  "outputDirectory": "apps/backend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
