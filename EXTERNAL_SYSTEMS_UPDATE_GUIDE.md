# External Systems Secret Update Guide

## 🔐 External Systems Requiring Secret Updates

### 1. GitHub Actions Secrets (HIGH PRIORITY)

**Required Updates:**
- `CF_API_TOKEN` - Cloudflare API token (new value from rotation)
- `CF_ACCOUNT_ID` - Cloudflare account ID (new value from rotation)
- `STRIPE_SECRET_KEY` - Stripe secret key (new value from rotation)

**Current Status:** 
- ✅ GitHub workflow files reference these secrets
- ⚠️ GitHub repository secrets still have OLD values
- ⚠️ Need to update in GitHub repository settings

**Update Steps:**
1. Go to https://github.com/atomeam/Aether/settings/secrets/actions
2. Find `CF_API_TOKEN` and update with new Cloudflare API token
3. Find `CF_ACCOUNT_ID` and update with new Cloudflare account ID
4. Find `STRIPE_SECRET_KEY` and update with new Stripe secret key
5. Test workflows to ensure they work with new secrets

**Impact:** Without this update, CI/CD workflows will fail

---

### 2. Stripe Dashboard (HIGH PRIORITY)

**Required Updates:**
- Stripe Secret Key (rotated, but may need dashboard cleanup)

**Current Status:**
- ✅ Cloudflare Workers secret updated
- ⚠️ Old Stripe key may still be active in Stripe dashboard
- ⚠️ Need to deactivate old key in Stripe dashboard

**Update Steps:**
1. Go to https://dashboard.stripe.com/apikeys
2. Find the old Stripe secret key
3. Click "Delete" or "Revoke" to deactivate it
4. Verify the new key is the only active key
5. Update any systems using the old key

**Impact:** Without this, old key could still be used maliciously

---

### 3. Google Account Password (CRITICAL)

**Required Updates:**
- Google account password `J@denb11` (found in git history)

**Current Status:**
- ✅ Removed from code
- ✅ Environment variable handling implemented
- ⚠️ Google account password still needs to be changed

**Update Steps:**
1. Go to https://myaccount.google.com/security
2. Sign in with current password `J@denb11`
3. Click "Password" under "Signing in to Google"
4. Change password to a strong, unique password
5. Update `.env` file with new `GOOGLE_PASSWORD` value
6. Update any devices/apps using the old password

**Impact:** Without this, account could be compromised

---

### 4. Kraken API Credentials (MEDIUM PRIORITY)

**Required Updates:**
- Kraken API Key (rotated in Cloudflare)
- Kraken API Secret (rotated in Cloudflare)

**Current Status:**
- ✅ Cloudflare Workers secrets updated
- ⚠️ Scripts use environment variables (already secure)
- ⚠️ May need to update if using Kraken dashboard directly

**Update Steps:**
1. Go to https://www.kraken.com/u/settings/api
2. Check if old API keys are still active
3. Deactivate old keys if still present
4. Scripts already use environment variables, so no code changes needed

**Impact:** Low - scripts already use environment variables

---

### 5. Notion API Token (MEDIUM PRIORITY)

**Required Updates:**
- Notion API token (rotated in Cloudflare)

**Current Status:**
- ✅ Cloudflare Workers secret updated
- ⚠️ May need to update if using Notion integration directly

**Update Steps:**
1. Check if any scripts use Notion API directly
2. If so, update with new token
3. Deactivate old token in Notion settings

**Impact:** Low - limited Notion integration

---

### 6. Sentry DSN (LOW PRIORITY)

**Required Updates:**
- Sentry DSN (rotated in Cloudflare)

**Current Status:**
- ✅ Cloudflare Workers secret updated
- ⚠️ May need to update if using Sentry dashboard directly

**Update Steps:**
1. Check if Sentry is configured in any dashboards
2. Update DSN if needed
3. Test error monitoring still works

**Impact:** Low - error monitoring

---

## 🎯 Priority Order

### IMMEDIATE (Do Now)
1. **Google Account Password** - `J@denb11` was exposed in git history
2. **GitHub Actions Secrets** - CI/CD workflows depend on these
3. **Stripe Dashboard** - Deactivate old secret key

### HIGH PRIORITY (This Week)
4. **Kraken API Credentials** - If actively using Kraken trading
5. **Notion API Token** - If actively using Notion integration

### LOW PRIORITY (This Month)
6. **Sentry DSN** - Error monitoring

---

## 📋 Update Checklist

- [ ] Change Google account password `J@denb11`
- [ ] Update GitHub Actions secret `CF_API_TOKEN`
- [ ] Update GitHub Actions secret `CF_ACCOUNT_ID`
- [ ] Update GitHub Actions secret `STRIPE_SECRET_KEY`
- [ ] Deactivate old Stripe secret key in dashboard
- [ ] Update `.env` file with new `GOOGLE_PASSWORD`
- [ ] Test GitHub Actions workflows after secret updates
- [ ] Verify all services work with new secrets

---

**Last Updated:** 2026-06-14
**Generated by:** Devin Security Audit
